type Todo @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  description: String
}

type Recipe @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  ingredients: String!
  instructions: String!
  Favorite: Boolean
}
 